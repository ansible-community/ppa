${DEB_NAME} (${DEB_CHANGELOG_VERSION}-${DEB_RELEASE}~${DIST}) ${DIST}; urgency=low

  * ${DEB_NAME} ${DEB_CHANGELOG_VERSION} release

 -- Ansible Community Builds <ansible-community-builds@redhat.com>  ${DATE}
